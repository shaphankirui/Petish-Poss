export interface Shift {
    id: any;
    TimeEnded: any;
    ShiftName: string;
    startTime: Date;
    // shiftId: number;
    shiftStarted: boolean;
    // Add other properties as needed for your shift data
  }
  